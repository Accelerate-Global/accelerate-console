import { SettingsPage } from "@/components/settings-page"

export const metadata = {
  title: "Settings | Accelerate Global // Data",
  description: "Manage your account and preferences.",
}

export default function Settings() {
  return <SettingsPage />
}
